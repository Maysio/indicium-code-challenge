plugins:
  extractors:
    - name: tap-csv
      config:
        files: ['./data/order_details.csv']
  loaders:
    - name: target-postgres
      config:
        host: localhost
        port: 5432
        user: northwind_user
        password: thewindisblowing
        dbname: northwind
